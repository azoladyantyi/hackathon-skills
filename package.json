{
  "name": "gh-trix",
  "version": "1.0.0",
  "description": "Pair with another coder and do this GitHub exercise. This exercise will show you a workflow that allows two uses to work together on a shared GitHub repository. There are other workflows like `git flow` which use feature branching, but this approach will help you to work on the same branch with one or more other developers. It will prepare you for using `git flow` approach in the future.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/avermeulen/gh-trix.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/avermeulen/gh-trix/issues"
  },
  "homepage": "https://github.com/avermeulen/gh-trix#readme",
  "dependencies": {
    "lodash": "^4.17.4"
  }
}
